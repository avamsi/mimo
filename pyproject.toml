[project]
name = "mimo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "marimo[mcp,recommended]>=0.19.4",
    "plotly[express]>=6.5.2",
]

[tool.marimo]
completion.copilot = "codeium"
display.default_width = "full"
runtime.watcher_on_save = "autorun"
