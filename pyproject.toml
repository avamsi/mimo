[project]
name = "mimo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "marimo[mcp,recommended]>=0.19.11",
    "pip-system-certs>=5.3",
    "plotly[express]>=6.5.2",
]

[project.scripts]
mimo = "mimo:main"

[tool.uv]
package = true
